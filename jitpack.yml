# configuration file for building snapshots and releases with jitpack.io
jdk:
  - openjdk17
install:
  - FILE="-Dfile=trackmyuser-0.2.2.aar"
  - mvn install:install-file $FILE -DgroupId=com.github.trackmyuser -DartifactId=android-sdk -Dversion=0.2.2 -Dpackaging=aar -DgeneratePom=true
